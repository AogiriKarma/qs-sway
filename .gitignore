# logs quickshell
*.qslog
# dumps de debug (swaymsg -t get_tree, etc.)
tree.json

# preferences utilisateur : les defauts vivent dans services/Config.qml
config.json

# fichiers de conception : utiles a cote, pas dans l'historique
*.excalidraw
